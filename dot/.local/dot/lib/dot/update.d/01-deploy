# vim: ft=zsh
#################################
# Deploy ansible playbook changes
#################################

_dot_update_exec() {
    # deploy all changes in the ansible playbook
    dot_deploy
}
