#!/usr/bin/env zsh
set -e
#####################################
# Play a sound on dunst notifications
#####################################

if [[ "$(playerctl status)" != "Playing" ]]; then
    dot-snd-play notification
fi
