#!/usr/bin/env zsh
#################################
# A somewhat fancy i3lock wrapper
#################################

# fork i3lock
i3lock -e --color=101010

# Make sure keyboard-related settings are well set across screen locks
xset r rate 200 50
xmodmap ~/.Xmodmap
