#!/usr/bin/env zsh
set -e
#####################################
# Play a sound on dunst notifications
#####################################

if [[ "$(playerctl status)" != "Playing" ]]; then
    play -q ${HOME}/.local/share/sounds/notification.wav
fi
